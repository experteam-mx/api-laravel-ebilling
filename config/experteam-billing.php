<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Redis key to find the number of errors by URL
    |--------------------------------------------------------------------------
    */
    'redis' => [
        'errors_count' => env('APPLICATION_NAME') . ':urlErrors:count',
        'active_index' => env('APPLICATION_NAME') . ':activeIndex',
    ],

    /*
    |--------------------------------------------------------------------------
    | List of URL's
    |--------------------------------------------------------------------------
    */
    'urls' => [
        //list of urls
    ],

    /*
    |--------------------------------------------------------------------------
    | List of URL's
    |--------------------------------------------------------------------------
    */
    'max_fails' => 10
];
